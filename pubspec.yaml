name: flutte_challange
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=3.0.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts:
  eva_icons_flutter:
  get: ^4.2.0
  google_sign_in: 
  firebase_auth: 
  cloud_firestore: 
  firebase_core: 
  firebase_storage:
  file_picker: ^4.5.0
  mime:
   #type of the file 
  path_provider:
  uuid: 
  flutter_image_compress:
   # compress images to be send to the firebase
  flutter_ffmpeg:
   # compress the videos with 
  video_compress: 
  url_launcher:
  audioplayers:
  chewie: #video playing 
  video_player: 
  flutter_downloader: ^1.7.1
  permission_handler: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
